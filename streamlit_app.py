# -*- coding: utf-8 -*-
"""streamlit_app.py

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1GNlG_QG8bUjcL4d-rMQKPEWJKbmxYgJ6
"""

pip install streamlit

import streamlit as st
import requests
from datetime import date

API_URL = "https://house-price-prediction-api-zj3e.onrender.com/predict"

st.set_page_config(
    page_title="House Price Prediction",
    page_icon="🏠",
    layout="centered"
)
st.markdown(
    """
    <h1 style='text-align:center; color:#2c3e50;'>🏠 House Price Prediction</h1>
    <h4 style='text-align:center; color:#34495e;'>
        ML-powered real estate valuation
    </h4>
    <br>
    """,
    unsafe_allow_html=True
)

st.info(
    "Enter property details below and click **Predict House Price** to get an estimated value."
)
with st.form("prediction_form"):

    col1, col2 = st.columns(2)

    with col1:
        size = st.number_input(
            "House Size (sq ft)",
            min_value=300,
            max_value=10000,
            step=50
        )

        bedrooms = st.number_input(
            "Bedrooms",
            min_value=1,
            max_value=10,
            step=1
        )

        bathrooms = st.number_input(
            "Bathrooms",
            min_value=1,
            max_value=10,
            step=1
        )

        year_built = st.number_input(
            "Year Built",
            min_value=1900,
            max_value=date.today().year,
            step=1
        )

    with col2:
        location = st.selectbox(
            "Location",
            ["CityA", "CityB", "CityC", "CityD"]
        )

        condition = st.selectbox(
            "Condition",
            ["Fair", "Good", "New"]
        )

        property_type = st.selectbox(
            "Property Type",
            ["Condominium", "Single Family", "Townhouse"]
        )

        date_sold = st.date_input(
            "Sale Date",
            value=date(2019, 1, 1)
        )

    st.markdown("<br>", unsafe_allow_html=True)

    submit_btn = st.form_submit_button(
        "Predict House Price",
        use_container_width=True
    )

if submit_btn:

    payload = {
        "location": location,
        "size": size,
        "bedrooms": bedrooms,
        "bathrooms": bathrooms,
        "year_built": year_built,
        "condition": condition,
        "type": property_type,
        "date_sold": date_sold.strftime("%Y-%m-%d")
    }
    with st.spinner("Predicting price..."):
        try:
            response = requests.post(API_URL, json=payload, timeout=20)

            if response.status_code == 200:
                result = response.json()

                if "predicted_price" in result:
                    predicted_price = result["predicted_price"]

                    st.markdown("<br>", unsafe_allow_html=True)

                    st.markdown(
                        f"""
                        <div style="
                            background-color:#ffffff;
                            padding:25px;
                            border-radius:12px;
                            box-shadow:0px 4px 12px rgba(0,0,0,0.15);
                            text-align:center;
                        ">
                            <h2 style="color:#27ae60;"> Estimated House Price</h2>
                            <h1 style="color:#2c3e50;">₹ {predicted_price:,.2f}</h1>
                        </div>
                        """,
                        unsafe_allow_html=True
                    )

                else:
                    st.error("Prediction failed. Unexpected API response.")

            else:
                st.error("Failed to get prediction from API.")

        except Exception as e:
            st.error(f"Error connecting to API: {e}")

st.markdown("<br>", unsafe_allow_html=True)

with st.expander("About this project"):
    st.markdown(
        """
        **House Price Prediction App**
        - Built using **Machine Learning (XGBoost Regressor)**
        - Trained on **log-transformed house prices** for stable & positive predictions
        - Backend API built with **FastAPI**
        - Deployed on **Render**
        - Frontend developed using **Streamlit**

        This project demonstrates an **end-to-end ML workflow**:
        data preprocessing → model training → API deployment → UI integration.
        """
    )

st.markdown(
    """
    <hr>
    <p style="text-align:center; color:gray;">
        Built with love using Python, FastAPI, Streamlit & Machine Learning
    </p>
    """,
    unsafe_allow_html=True
)